import { generatePrediction } from "@/actions/surveillance"; import { PageHeader, RiskBadge } from "@/components/dashboard-ui"; import prisma from "@/lib/prisma";
export default async function Page(){const rows=await prisma.prediction.findMany({orderBy:{createdAt:"desc"},take:30,include:{model:true}});return <><PageHeader eyebrow="AI prediction engine" title="Outbreak predictions" description="Combine validated case trends and the latest environmental readings to estimate 30-day district risk."/><form action={generatePrediction} className="mb-8 flex flex-wrap gap-3 rounded border bg-white p-5"><input required name="province" placeholder="Province" className="rounded border px-3 py-2.5 text-sm"/><input required name="district" placeholder="District" className="rounded border px-3 py-2.5 text-sm"/><button className="rounded bg-slate-900 px-5 py-2.5 text-sm font-bold text-white">Generate prediction</button></form><div className="grid gap-4">{rows.map(p=><article key={p.id} className="rounded border bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-bold">{p.district}, {p.province}</h2><p className="mt-1 text-xs text-slate-500">{p.model.name} v{p.model.version} · {p.predictionDate.toLocaleDateString()}</p></div><RiskBadge risk={p.riskLevel}/></div><div className="mt-5 grid grid-cols-3 gap-4"><div><p className="text-xs text-slate-500">Probability</p><p className="text-2xl font-bold">{Math.round(p.outbreakProbability*100)}%</p></div><div><p className="text-xs text-slate-500">Expected cases</p><p className="text-2xl font-bold">{p.predictedCaseCount}</p></div><div><p className="text-xs text-slate-500">Confidence</p><p className="text-2xl font-bold">{Math.round(p.confidenceScore*100)}%</p></div></div><p className="mt-5 rounded bg-slate-50 p-4 text-sm leading-6">{p.recommendation}</p></article>)}</div></>}
