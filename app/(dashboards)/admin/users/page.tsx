import prisma from "@/lib/prisma"; import { updateUserRole } from "@/actions/admin"; import { PageHeader } from "@/components/dashboard-ui";
export default async function Page(){const users=await prisma.user.findMany({orderBy:{createdAt:"desc"},take:100});return <><PageHeader eyebrow="Access control" title="Users and roles" description="Manage Administrator, Health Officer, and Government Official access."/><div className="overflow-hidden rounded border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">User</th><th>Email</th><th>Role</th><th>Verified</th><th className="p-4">Change role</th></tr></thead><tbody className="divide-y">{users.map(u=><tr key={u.id}><td className="p-4 font-semibold">{u.name}</td><td>{u.email}</td><td>{u.role?.replaceAll("_"," ")}</td><td>{u.emailVerified?"Yes":"No"}</td><td className="p-4"><form action={updateUserRole.bind(null,u.id)} className="flex gap-2"><select name="role" defaultValue={u.role??"health_officer"} className="rounded border px-2 py-1"><option value="health_officer">Health Officer</option><option value="government_official">Government Official</option><option value="admin">Administrator</option></select><button className="font-bold text-slate-900">Save</button></form></td></tr>)}</tbody></table></div></>}
