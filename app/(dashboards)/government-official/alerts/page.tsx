import { acknowledgeAlert } from "@/actions/surveillance"; import prisma from "@/lib/prisma"; import { PageHeader, RiskBadge, EmptyState } from "@/components/dashboard-ui"; import { predictionLocationPath } from "@/lib/location";
export default async function Page(){const rows=await prisma.alert.findMany({orderBy:{issuedAt:"desc"},include:{prediction:true}});return <><PageHeader eyebrow="National response" title="Priority alerts" description="Monitor and acknowledge outbreak warnings requiring government awareness or coordination."/><div className="space-y-4">{rows.map(a=><article key={a.id} className="rounded border bg-white p-6"><div className="flex flex-wrap justify-between gap-4"><div><div className="flex gap-3"><h2 className="font-bold">{a.title}</h2><RiskBadge risk={a.riskLevel}/></div><p className="mt-2 text-sm text-slate-500">{a.prediction?predictionLocationPath(a.prediction):[a.province,a.district].filter(Boolean).join(" / ")}</p><p className="mt-4 text-sm">{a.message}</p></div>{a.status==="ACTIVE"&&<form action={acknowledgeAlert.bind(null,a.id)}><button className="rounded border border-slate-900 px-4 py-2 text-sm font-bold text-slate-900">Acknowledge</button></form>}</div></article>)}{!rows.length&&<EmptyState>No national alerts currently require attention.</EmptyState>}</div></>}
