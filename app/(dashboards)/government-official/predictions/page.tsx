import prisma from "@/lib/prisma"; import { PageHeader, RiskBadge, EmptyState } from "@/components/dashboard-ui";
export default async function Page(){const rows=await prisma.prediction.findMany({orderBy:{predictionDate:"desc"},take:100,include:{model:true}});return <><PageHeader eyebrow="National risk analysis" title="Regional predictions" description="Compare district-level outbreak probabilities, confidence, expected burden, and recommended response."/><div className="grid gap-4 lg:grid-cols-2">{rows.map(p=><article key={p.id} className="rounded border bg-white p-6"><div className="flex justify-between"><div><h2 className="font-bold">{p.district}, {p.province}</h2><p className="text-xs text-slate-500">{p.model.name} v{p.model.version}</p></div><RiskBadge risk={p.riskLevel}/></div><div className="mt-5 flex gap-8"><p><strong className="block text-2xl">{Math.round(p.outbreakProbability*100)}%</strong><span className="text-xs text-slate-500">probability</span></p><p><strong className="block text-2xl">{p.predictedCaseCount}</strong><span className="text-xs text-slate-500">expected cases</span></p></div><p className="mt-5 text-sm leading-6 text-slate-600">{p.recommendation}</p></article>)}{!rows.length&&<EmptyState>No regional predictions available.</EmptyState>}</div></>}
