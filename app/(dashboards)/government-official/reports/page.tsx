import prisma from "@/lib/prisma"; import { PageHeader, EmptyState } from "@/components/dashboard-ui";
export default async function Page(){const rows=await prisma.report.findMany({where:{type:{in:["NATIONAL","REGIONAL","SUMMARY","ANNUAL"]}},orderBy:{createdAt:"desc"}});return <><PageHeader eyebrow="Policy reporting" title="Government reports" description="National, regional, annual, and summarized reports prepared for public-health decisions."/>{rows.length?<div className="rounded border bg-white">{rows.map(r=><div key={r.id} className="flex justify-between border-b p-5"><div><p className="font-bold">{r.title}</p><p className="text-xs text-slate-500">{r.type} · {r.createdAt.toLocaleDateString()}</p></div><span className="text-sm font-bold">{r.status}</span></div>)}</div>:<EmptyState>No government reports are ready yet.</EmptyState>}</>}
