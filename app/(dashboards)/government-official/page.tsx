import prisma from "@/lib/prisma"; import { PageHeader, RiskBadge, StatCard } from "@/components/dashboard-ui";
export default async function Page(){const [cases,predictions,highRisk,alerts,regions]=await Promise.all([prisma.diseaseCase.count({where:{validationStatus:"VALIDATED"}}),prisma.prediction.count(),prisma.prediction.count({where:{riskLevel:{in:["HIGH","CRITICAL"]}}}),prisma.alert.count({where:{status:"ACTIVE"}}),prisma.prediction.findMany({orderBy:{predictionDate:"desc"},distinct:["district"],take:10})]);return <><PageHeader eyebrow="Government decision centre" title="National typhoid outlook" description="A high-level view of validated surveillance data, regional outbreak probability, and active response priorities."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Validated cases" value={cases} detail="National surveillance total"/><StatCard label="Predictions" value={predictions} detail="All district assessments"/><StatCard label="High-risk signals" value={highRisk} detail="High and critical classifications" trend={highRisk?"up":undefined}/><StatCard label="Active alerts" value={alerts} detail="Awaiting coordinated response"/></div><section className="mt-8 overflow-hidden rounded border bg-white"><div className="border-b p-5"><h2 className="font-bold">Latest regional risk</h2></div><div className="divide-y">{regions.map(p=><div key={p.id} className="flex items-center justify-between p-5"><div><p className="font-semibold">{p.district}, {p.province}</p><p className="mt-1 text-xs text-slate-500">{p.predictedCaseCount} expected cases · {Math.round(p.outbreakProbability*100)}% probability</p></div><RiskBadge risk={p.riskLevel}/></div>)}</div></section></>}
