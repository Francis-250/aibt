"use client";
import { useState } from "react"; import Link from "next/link"; import { authApiRequest } from "@/lib/auth-api-client";
export default function ForgotPasswordPage(){const [email,setEmail]=useState("");const [pending,setPending]=useState(false);const [message,setMessage]=useState("");async function submit(event:React.FormEvent){event.preventDefault();setPending(true);const result=await authApiRequest("/api/auth/forgot-password",{email});setPending(false);setMessage(result.message||"If an account exists for that email, a password-reset link has been sent.")}return <><p className="text-xs font-medium text-blue-700">Account recovery</p><h1 className="mt-1 text-2xl font-semibold">Reset password</h1><p className="mb-4 mt-1 text-xs text-slate-500">Enter the email connected to your account.</p><form onSubmit={submit} className="space-y-3"><label className="block text-xs font-semibold">Work email<input value={email} onChange={event=>setEmail(event.target.value)} type="email" required className="mt-1 w-full rounded border border-slate-300 px-3 py-2 text-sm"/></label>{message&&<p className="rounded bg-slate-50 p-3 text-xs text-slate-600">{message}</p>}<button disabled={pending} className="w-full rounded bg-slate-950 px-3 py-2.5 text-sm font-semibold text-white">{pending?"Sending…":"Send reset link"}</button><p className="text-center text-xs"><Link href="/auth/login" className="text-blue-700">Return to sign in</Link></p></form></>}
