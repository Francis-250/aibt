"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { authApiRequest } from "@/lib/auth-api-client";
export function ResetPasswordForm({token}:{token:string}){const router=useRouter();const [error,setError]=useState("");const [pending,setPending]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const password=String(data.get("password"));if(password!==String(data.get("confirmation"))){setError("Passwords do not match.");return}setPending(true);const result=await authApiRequest("/api/auth/reset-password",{token,password});if(!result.ok){setError(result.error||"Unable to reset password.");setPending(false);return}router.replace("/auth/login?reset=success")}const input="mt-1 w-full rounded border border-slate-300 px-3 py-2 text-sm";return <form onSubmit={submit} className="space-y-3"><label className="block text-xs font-semibold">New password<input name="password" type="password" minLength={8} required className={input}/></label><label className="block text-xs font-semibold">Confirm password<input name="confirmation" type="password" minLength={8} required className={input}/></label>{error&&<p className="text-xs text-rose-600">{error}</p>}<button disabled={pending} className="w-full rounded bg-slate-950 px-3 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{pending?"Updating…":"Update password"}</button></form>}
