import { AlertTriangle, ArrowDownRight, ArrowUpRight } from "lucide-react";
import { cn } from "@/lib/utils";

export function PageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.2em] text-slate-900">{eyebrow}</p><h1 className="text-3xl font-bold tracking-tight">{title}</h1><p className="mt-2 max-w-2xl text-sm text-slate-500">{description}</p></div>{action}</div> }
export function StatCard({ label, value, detail, trend }: { label: string; value: string | number; detail: string; trend?: "up"|"down" }) { return <div className="rounded border bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><div className="mt-2 flex items-center justify-between"><p className="text-3xl font-bold">{value}</p>{trend === "up" ? <ArrowUpRight className="text-rose-500"/> : trend === "down" ? <ArrowDownRight className="text-slate-700"/> : null}</div><p className="mt-2 text-xs text-slate-400">{detail}</p></div> }
export function RiskBadge({ risk }: { risk: string }) { const styles:Record<string,string>={LOW:"bg-sky-100 text-sky-800",MODERATE:"bg-amber-100 text-amber-800",HIGH:"bg-orange-100 text-orange-800",CRITICAL:"bg-rose-100 text-rose-800"}; return <span className={cn("inline-flex items-center gap-1 rounded px-2.5 py-1 text-xs font-bold",styles[risk]??"bg-slate-100")}><AlertTriangle size={12}/>{risk}</span> }
export function EmptyState({ children }: { children: React.ReactNode }) { return <div className="rounded border border-dashed bg-white px-6 py-16 text-center text-sm text-slate-500">{children}</div> }
